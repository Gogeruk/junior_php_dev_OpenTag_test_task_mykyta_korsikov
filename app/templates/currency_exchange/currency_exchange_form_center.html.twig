{% extends 'currency_exchange/center_div.html.twig' %}

{% block center_div %}
    {% if failed_to_exchange %}
        <a class="m-2" href="{{ path('app_currency_index') }}">Failed to Exchange</a>
    {% else %}
        {{ component('currency_exchange_form', {
            form: form
        }) }}
    {% endif %}
{% endblock %}


